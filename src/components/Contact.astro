<section id="contact" class="contact-section">
  <div class="section-shell contact-grid">
    <div>
      <div class="section-kicker">/04 Den Den Mushi</div>
      <h2>REACH THE CREW.</h2>
      <p>
        Open to opportunities, collaborations, and cursed technical conversations.
        Si hay una buena aventura, manda señal.
      </p>
    </div>


    <div class="contact-panel hard-panel">
      <a href="mailto:valentingb19@outlook
      .com" class="red-button">Email</a>
      <a href="https://github.com/vgb95" target="_blank" rel="noopener noreferrer" class="ghost-button">GitHub</a>
      <a href="https://www.linkedin.com/in/valentin-gonzalez-4b8a75a8/" target="_blank" rel="noopener noreferrer" class="ghost-button">LinkedIn</a>

      <div class="footer-note">
        <span class="scar-line" aria-hidden="true"></span>
        <p>© 2026 Valentín González · Built with Astro + Tailwind · Inspired by luffy.sh, sailing under red hair energy.</p>
      </div>
    </div>
  </div>
</section>

<style>
  .contact-section {
    padding-bottom: 2rem;
  }

  .contact-grid {
    display: grid;
    grid-template-columns: 1fr minmax(20rem, 0.85fr);
    gap: clamp(2rem, 6vw, 5rem);
    align-items: center;
  }

  h2 {
    max-width: 9ch;
    font-size: clamp(3.5rem, 9vw, 8rem);
    font-weight: 900;
  }

  p {
    max-width: 34rem;
    margin-top: 1rem;
    color: var(--muted);
    font-size: clamp(1rem, 2vw, 1.35rem);
    font-weight: 800;
  }

  .contact-panel {
    display: grid;
    gap: 0.9rem;
    padding: clamp(1.1rem, 3vw, 2rem);
  }

  .footer-note {
    margin-top: 1rem;
    border-top: 2px solid var(--line);
    padding-top: 1rem;
  }

  .footer-note p {
    margin-top: 0.7rem;
    font-size: 0.76rem;
    font-weight: 900;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  @media (max-width: 780px) {
    .contact-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
